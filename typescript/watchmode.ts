 // by  using it we do not need to type same command multiple times like tsc filename.ts after every change whenever we will save it will automatically be converted to .js file

 // to use watch mode we should use command -> tsc filename.ts --watch

let a:number=2908343;
let b:string='Mohit';
console.log(a);


// to run multiple files together first install configuration file using command ts -watch and then type tsc -w or tsc --watch